// TODO: Implement password hashing and comparison functions

export function hashPassword (password: string) {
  return password
}

export function comparePassword (password: string, hashedPassword: string) {
  return password === hashedPassword
}
